function ${field}Formatter(value) {
  #if($codeList.size()>0)
  #foreach($code in $codeList)
  	if (value == '${code.dictCode}') #if("$!code.showColor"=="")return '${code.dictValue}'#else return '<span style="color:${code.showColor}">${code.dictValue}'#end ;
  #end 
    else return value;
  #else
  return value;
  #end
}